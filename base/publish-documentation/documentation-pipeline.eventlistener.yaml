apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: documentation-gitlab-listener
spec:
  serviceAccountName: pipeline
  triggers:
    - name: gitlab-push-events-trigger
      interceptors:
        - name: "documentation-pipeline"
          ref:
            name: "gitlab"
          params:
            - name: "secretRef"
              value:
                secretName: foo
                secretKey: bar
            - name: "eventTypes"
              value: ["Push Hook"]
      bindings:
        - kind: TriggerBinding
          ref: documentation-pipeline-binding
      template:
        ref: documentation-pipeline-template
