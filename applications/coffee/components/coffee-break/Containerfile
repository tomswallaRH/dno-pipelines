# UBI image — Konflux build clusters often deny docker.io pulls; this registry is typically allowed.
FROM registry.access.redhat.com/ubi9/python-312:latest

WORKDIR /app

COPY src/app.py .

EXPOSE 8080

CMD ["/usr/bin/python3", "/app/app.py"]
